from types import SimpleNamespace


retorne_select: SimpleNamespace = SimpleNamespace()

# Relacionadas ao arquivo 'select_main.py'
retorne_select.atributo_str = 'O atributo tabela deve ser do tipo "str".'
retorne_select.tabela_nao_existe = 'A tabela "{tabela}" não existe!'
